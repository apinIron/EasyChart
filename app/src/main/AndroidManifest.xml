<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.iron.easychart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".Style1Activity"/>

        <activity android:name=".Style2Activity"/>

        <activity android:name=".Style3Activity"/>

        <activity android:name=".PieStyle1Activity"/>

        <activity android:name=".PieStyle2Activity"/>

        <activity android:name=".PieStyle3Activity"/>

    </application>

</manifest>